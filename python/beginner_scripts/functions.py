#!/usr/bin/env python

def ifState():
	a = 10;	
	if a > 10:
		print "A is bigger than 10."
	elif a < 10:
		print "A is smaller than or equal to 10."
	else :
		print "A is not a number."
		 
ifState()
